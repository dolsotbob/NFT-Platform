import loading from '../assets/loading.gif';

const Loading = () => {
    return (
        <div>
            <img src={loading} alt="loading"></img>
        </div>
    );
};

export default Loading; 
